# Issue Tracker

This project uses **GitHub Issues** as its issue tracker.

- Issues live in the repo's GitHub Issues: `https://github.com/m0shiurX/FiberOS/issues`
- PRs are not a request surface (external PRs are not triaged into the queue).
