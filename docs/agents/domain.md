# Domain Docs

This is a **single-context** repo. Domain documentation lives at the repo root:

- `CONTEXT.md` — the project's domain glossary, architecture, and conventions
- `docs/adr/` — Architecture Decision Records (one per decision)

Consumers should read `CONTEXT.md` and relevant ADRs before making changes that affect domain logic or architecture.
